# oxide-perf-runner::lib

## Intention and purpose

`oxide-perf-runner` owns the persisted Rust-side performance contract for Oxide. It exists to benchmark the engine, representative flows, author-facing APIs, and bridge paths under one regression-gated report format instead of leaving performance validation to ad hoc local timing.

## Relation to the rest of the code

- Upstream callers:
  - `oxide/crates/perf-runner/src/main.rs` invokes `run_from_env()`.
  - Local verification commands route through `cargo run -p oxide-perf-runner -- --run-suite ...`.
- Downstream dependencies:
  - `oxide_ui_core`, `oxide_renderer_metal`, `oxide_test_scenes`, `oxide_platform_api`, `oxide_platform_web`, and related crates provide the actual workload surfaces being measured.
  - `oxide/benchmarks/workspace/latest.json` and `oxide/benchmarks/workspace/latest.md` are the persisted outputs consumed by local comparison and review.

## Entry points list

- `oxide_perf_runner::run_from_env() -> anyhow::Result<()>`
  - Parses CLI arguments from the current process and dispatches into either the persisted suite flow or the legacy summary flow.
  - Main callers: `oxide/crates/perf-runner/src/main.rs`.
- `oxide_perf_runner::run_cli(args: &[String]) -> anyhow::Result<()>`
  - Handles the suite CLI, baseline writes, comparisons, and legacy fallback.
  - Also exposes `--paired-run PLAN --paired-json-out PATH`, which executes fresh-process workspace CPU, Metal, WebGPU, browser-startup, or device A/B commands through the shared source/artifact identity and raw-evidence contract, plus `--paired-analyze INPUT --paired-json-out PATH` for byte-deterministic reanalysis and `--paired-create-instrumentation-patch` for a hashed binary patch applied identically to both sides.
  - Also exposes `--bench-markdown-render PATH [--bench-markdown-compare PATH] [--bench-markdown-iters N]`, a non-default measurement harness that loads an existing `PerfReport` JSON and optional comparison baseline, then repeatedly renders Markdown so report-generation changes can be A/B tested without rerunning the suite workloads.
  - Also exposes `--bench-markdown-write PATH [--bench-markdown-compare PATH] [--bench-markdown-iters N]`, a non-default measurement harness that loads an existing `PerfReport` JSON and optional comparison baseline, then repeatedly exercises the latest-plus-dated Markdown output path so baseline-write changes can be A/B tested without rerunning the suite workloads.
  - Also exposes `--bench-json-render PATH [--bench-json-iters N]`, a non-default measurement harness that loads an existing `PerfReport` JSON and repeatedly exercises the shared pre-sized pretty JSON serializer used by the persisted JSON write path so report schema and artifact-density changes can be A/B tested without rerunning the suite workloads.
  - Also exposes `--bench-json-string-render PATH [--bench-json-iters N]`, a non-default measurement harness that loads an existing `PerfReport` JSON and repeatedly exercises the `collect_suite_json`-style String-return pretty JSON serializer so host-facing JSON export changes can be A/B tested without rerunning suite workloads.
  - Also exposes `--bench-sample-summary [--bench-sample-summary-iters N]`, a non-default measurement harness that repeatedly summarizes representative 6-, 10-, 12-, and 24-sample slices and reports summary counts plus a deterministic checksum so sample-summary allocation changes can be A/B tested without rerunning suite workloads.
  - Also exposes `--bench-case-filter [--bench-case-filter-iters N]`, a non-default measurement harness that repeatedly checks representative case IDs and family prefixes so case-selection changes can be A/B tested without running the suite workloads.
  - Also exposes `--bench-compare-reports CURRENT BASELINE [--bench-compare-iters N]`, a non-default measurement harness that loads two existing `PerfReport` JSON files, repeatedly runs `compare_reports`, and reports comparison counts plus a deterministic checksum so lookup-table changes can be A/B tested without rerunning suite workloads.
  - Also exposes `--bench-frame-pacing-metrics [--bench-frame-pacing-iters N]`, a non-default measurement harness that repeatedly inserts frame-pacing metrics for a representative interval slice and reports metric counts plus a deterministic checksum so missed-frame and hitch metric insertion changes can be A/B tested without rerunning suite workloads.
  - Also exposes `--bench-distribution-metrics [--bench-distribution-iters N]`, a non-default measurement harness that repeatedly inserts representative `frame_ms`, `event_to_visible_ms`, and `gpu_ms` distribution metrics and reports metric counts plus a deterministic checksum so distribution metric-key changes can be A/B tested without rerunning suite workloads.
  - Also exposes `--bench-case-metric-contract PATH [--bench-case-metric-iters N]`, a non-default measurement harness that loads an existing `PerfReport` JSON, repeatedly validates the case metric contract, and reports case/required counts plus a deterministic checksum so required-key validation changes can be A/B tested without rerunning suite workloads.
  - Also exposes `--bench-contract-coverage PATH [--bench-contract-iters N]`, a non-default measurement harness that loads an existing `PerfReport` JSON, repeatedly validates contract coverage, and reports layer/battery/note counts plus a deterministic checksum so contract coverage validation changes can be A/B tested without rerunning suite workloads.
  - Main callers: tests and the binary entry point.
- `oxide_perf_runner::compare_reports(current: &PerfReport, baseline: &PerfReport) -> PerfComparison`
  - Applies regression gating against the persisted baseline.
  - Main callers: suite execution and report tests.
- `oxide_perf_runner::assert_full_coverage(coverage: &CoverageReport) -> anyhow::Result<()>`
  - Enforces that every required battery family has an implemented case.
  - Main callers: suite execution and report tests.
- `oxide_perf_runner::assert_contract_coverage(contract: &ContractCoverageReport) -> anyhow::Result<()>`
  - Enforces stable contract coverage status values and rejects implemented rows whose notes still describe unresolved gaps.
  - Main callers: suite execution and report tests.
- `oxide_perf_runner::assert_case_metric_contract(cases: &[PerfCaseResult]) -> anyhow::Result<()>`
  - Enforces first-class frame and GPU metric distributions for report rows that claim frame timing or GPU frame timing.
  - Main callers: suite execution and report tests.
- `gpu.authoring.retained_snapshot.clean_mixed`
  - Exercises the public `MetalRenderer::encode_snapshot` authoring boundary with the same dense immutable mixed-content packet as the C24 architecture contract.
- `persisted_report_root_and_case_schemas_are_frozen`
  - Test-only report gate that validates the persisted workspace, Oxide-device, UIKit-device, and browser WebGPU reports keep their top-level JSON schemas and required case-row keys stable before future report-schema extraction or code generation work.
- `persisted_report_case_id_sets_are_frozen`
  - Test-only report gate that validates the persisted workspace, Oxide-device, UIKit-device, and browser WebGPU report case-id sets stay stable by checking case counts plus compact deterministic digests.
- `persisted_web_report_subobject_schemas_are_frozen`
  - Test-only report gate that validates high-value browser WebGPU section keys stay stable for backend-path coverage, draw-family counters, glyph counters, layer/effect counters, upload counters, WASM allocation audit, warm-resource churn, timestamp attribution, and timestamp stage breakdown.
- `persisted_report_nested_key_sets_are_frozen`
  - Test-only report gate that validates nested workspace, Oxide-device, UIKit-device, and browser WebGPU report section keys, metric key classes, and web case key classes stay stable while schema extraction work is prepared.
- `persisted_workspace_native_renderer_metric_keys_are_frozen`
  - Test-only report gate that validates native workspace renderer metric key sets stay stable for GPU scenes, id-mask, animation refresh matrix, collection navigation, retained Scene3D authoring, and PNG first-visible frame rows.
- `perf_runner_docs_define_schema_versioning_rules`
  - Test-only documentation gate that validates this file keeps explicit schema-versioning rules for persisted reports, benchmark IDs, metric keys, web sections, device rows, ABI evidence, and legacy-row retirement.
- `web_latest_report_satisfies_webgpu_distribution_and_pacing_contract`
  - Test-only report gate that validates `benchmarks/web/latest.json` keeps the 17-row default browser WebGPU matrix populated with p50/p95/p99/peak, missed-frame, hitch, resource-lifetime counters, draw-family counters, layer cache counters, image-upload temp/scratch counters, current upload summaries with direct glyph/RGBA timestamp totals, current clean-layer cache-hit/skipped-draw counters, effect-uniform write/byte/slot counters plus direct GPU timestamp totals, current backdrop-batch texture-copy/render-pass counters, current neon-marker marker/state counters, current direct-surface one-pass/timestamp counters, pass-family counters, timestamp-query attribution status, GPU timestamp stage breakdown with row reconciliation, Chrome browser trace summary fields, per-benchmark User Timing labels, per-benchmark trace intervals with scoped event/GPU/WebGPU counts from a duplicate benchmark-report run, retained current summaries, explicit backend-path coverage rows, warm-resource-churn summaries, current-row Rust/WASM allocation counters, frame-loop and submit-stage allocation attribution, zero WASM memory growth plus Chrome JS heap sampling across benchmark marks after prewarm, and pixel-check fields.
- `workspace_latest_gates_mac_metal_animation_and_navigation_pacing_rows`
  - Test-only report gate that validates `benchmarks/workspace/latest.json` keeps the macOS Metal animation refresh-matrix and collection-navigation frame-pacing rows populated with direct GPU distributions, frame distributions, missed-frame/hitch metrics, refresh-mode metadata, and workload diagnostics.
- `workspace_latest_gates_retained_layout_dirty_class_rows`
  - Test-only report gate that validates retained-surface authoring and dirty-class layout rows keep zero-layout, retained-reuse, retained-rebuild, dirty-node, and text-atlas context counters populated in `benchmarks/workspace/latest.json`.
- `workspace_latest_gates_collection_identity_and_prefix_ab_rows`
  - Test-only report gate that validates keyed collection reconciliation, bounded measurement-cache churn, and variable-prefix repair rows keep indexed-vs-scan, cold-repair, dirty-range, revision-query, and measurement-cache diagnostics populated in `benchmarks/workspace/latest.json`.
- `workspace_latest_gates_text_cache_atlas_and_cursor_rows`
  - Test-only report gate that validates the current wrapped-label and picker text cache rows, atlas pressure/dirty-upload rows, shaped cursor-map rows, and absence of the retired text audit rows in `benchmarks/workspace/latest.json`.

## Logic narrative

C60 registers the renderer-independent `ImageStore` architecture scaling matrix and the public `ImageRegionView` scroll/release/reuse authoring journey through the same filter and report pipeline.

The crate builds a case inventory spanning component/animation microbenchmarks, primitive lifecycle workloads, scene and journey flows, author-facing API workloads, state-reconciliation workloads, and bridge workloads. Primitive lifecycle coverage now includes empty-root mount, flat-rect mount/mutate/remove-all/remount, label mount/mutate, card mount/mutate, image mount/mutate, and a shared control-set slice so the report can speak to creation, invalidation, teardown, and remount costs instead of only encode hot paths. The system inventory includes text shaping, one-shaped-run prefix width mapping, batched fallback-font label encoding, cached wrapped-label fitting, atlas bake, constrained glyph-atlas pressure, and dirty-rect atlas upload publication so cursor cache misses, fallback glyph segmentation, wrapped-label cache misses, atlas eviction, and incremental glyph texture uploads remain part of the persisted hot-path contract. The layout inventory covers full relayout cases plus `cpu.layout.dirty_subtree.incremental_relayout`, which records per-node visit, skip, update, and child-measure counters for a one-cell layout mutation, `cpu.layout.descendant_only.incremental_relayout`, which isolates fixed-outer internal subtree edits that should avoid parent child-measure scans, `cpu.layout.transform_only.reposition`, which verifies transform-only changes keep layout counters at zero while retained draw/hit-test state updates, `cpu.layout.paint_only.opacity_clip`, which verifies opacity/clip dirty-class edits skip layout while retained sibling subtrees replay, `cpu.layout.node_content_dirty.retained_replay`, which verifies text/image/camera content dirty classes stay node-scoped, `cpu.layout.non_draw_dirty.retained_reuse`, which verifies accessibility/hit-test metadata dirtiness keeps layout and draw caches clean, and `cpu.layout.scoped_tree_mutation.add_remove`, which verifies common structural edits stay scoped to the changed branch. The state-reconciliation inventory includes single-node, 1 percent, 10 percent, and full-theme retained-tree mutation cases so diff/apply costs are visible as their own contract family instead of being inferred from primitive or router rows. The text-input inventory includes large-editor typing/paste/selection paths, a focused IME composition commit cycle, and LTR, RTL, fallback-font, plus mixed-bidi affinity cursor-pick cluster-map microcases for long Unicode single-line input backed by `oxide_text::ShapedCursorMap`; those rows persist cursor count, byte-boundary count, boundary checksum, affinity splits, width span, and fallback run count where applicable. The journey inventory includes a routed IME composition flow through the input scene. Collection journey rows persist measurement-call totals, item-revision query totals, and whether a collection revision hint was active so variable prefix reuse remains visible in reports. The authoring inventory also keeps `cpu.authoring.surface_router.compose` aligned with the public popup lifecycle API by exercising key-popup lookup, touch-region refresh, approval-gated dismissal, and retained current/overlay/popup composition metrics through `SurfaceRouter`, keeps retained-surface clean and dirty-leaf encodes represented with explicit subtree reuse metrics, measures retained surface replay through a live `TextCtx` atlas snapshot path, measures public `UiSurface` retained CPU/prepared-GPU budget configuration through `cpu.authoring.surface_retained.cache_policy`, measures checked multi-atlas retained text draw-list replay through the public `DrawListBuilder` API, A/B measures keyed collection focus reconciliation through indexed lookup versus scan fallback, measures bounded variable collection measurement-cache repair after large key churn, and A/B measures variable collection prefix repair through dirty-range updates versus full signature scans. The underlying `CollectionView` measurement cache is bounded, so these rows validate warm prefix reuse and cold-span repair without allowing unbounded historical key/revision retention. Each case is measured into a `PerfCaseResult` that carries both latency distributions and contract metadata such as layer, scenario, variant, cache state, and refresh mode. The `gpu.authoring.retained_snapshot.clean_mixed` row additionally changes only snapshot property data across clean frames and persists the prepared-cache counters needed to prove that app-authored immutable chunks do not relower or upload geometry.

CPU-oriented cases run through a shared warmup-and-sample loop that amortizes timer noise by executing enough operations to hit a target sample duration. Journey-style workloads run one full interaction cycle per sample. GPU scene and collection-navigation journey cases construct the normal three-slot visible Metal mode, execute live frames, and persist frame-resource depth/ring bytes/growth/backpressure alongside draw, encode, cull, damage, event-to-visible, direct GPU, missed-frame, and hitch distributions. The architecture matrix separately retains explicit eight-slot offscreen stress. The non-default sample-summary harness repeatedly summarizes the small sample counts used by smoke, journey, CPU, and GPU rows so summary-allocation and quantile changes can collect same-workload A/B proof without rerunning the suite workloads; common slices now sort in a fixed stack buffer with `sort_unstable_by` and fixed 6-, 10-, 12-, and 24-sample summaries use direct interpolation because same-workload proof showed lower summary cost while preserving min/max/mean and p50/p95/p99 checksums. Any row reported as `ms/frame` is contract-gated for frame p50/p95/p99/peak plus 60 Hz and 120 Hz missed-frame and hitch metrics, and GPU frame rows are additionally gated for GPU p50/p95/p99/peak distributions. The animation/effects battery includes a dedicated Metal refresh-matrix row that persists direct GPU distributions plus 60 Hz and 120 Hz missed-frame and hitch metrics for the animated scene. The authoring battery now also includes a retained `scene3d` mixed-frame case that renders a persistent 3D mesh pass ahead of a 2D overlay so the Metal backend's shared-frame 2D/3D path stays under regression coverage.

Bridge coverage includes both app-owned OS bridge workloads and the web backend surface available on native fallback builds. The web case exercises browser-backend capabilities, device caps, clipboard cache, network status, permission callbacks, location fallback, haptics, and the explicit unsupported iframe WebView boundary so the WebAssembly backend is represented in the workspace contract even when the browser-only DOM paths are tested separately.

The resulting `PerfReport` is serialized to JSON and Markdown. Baseline comparison is median-based and only applies to gated cases. Coverage validation is structural: the suite is considered incomplete if any required family lacks a case. The non-default JSON render harness loads an existing JSON report, then times repeated calls through the shared pre-sized pretty JSON serializer, which writes with `serde_json::to_writer_pretty` into a report-shape-sized `Vec`, so pretty JSON serialization, report schema, and artifact-density changes can collect same-workload A/B proof without rerunning the suite workloads. The non-default JSON String render harness loads the same report shape and times the `collect_suite_json`-style String-return serializer, so host-facing JSON export changes can be measured separately from persisted file writes. Persisted JSON writes use the same serializer because the current workspace report proved the pre-sized path lowers JSON render cost while preserving byte-identical output. The serializer capacity hint is tightened around the current report shape because the same workload proved the smaller hint lowers JSON render cost while preserving `158970` output bytes. The non-default Markdown render harness loads an existing JSON report and can optionally load a comparison baseline, then times repeated `render_markdown` calls, which isolates report-generation work for same-workload A/B proof when later schema extraction or stale-report-branch cleanup touches Markdown rendering. The non-default Markdown write harness uses the same report and optional comparison baseline but exercises the latest-plus-dated Markdown output path, which isolates baseline-write rendering work for same-workload A/B proof without filesystem noise or suite reruns. Baseline Markdown writes render the latest-plus-dated body once and write those same bytes to both files, because the same-workload write harness proved that removing the duplicate render about halves the repeated write-path render cost while preserving bytes and comparison counts. The non-default compare-reports harness loads current and baseline JSON reports once, repeatedly runs `compare_reports`, and reports matched/regression/missing/improvement counts plus a deterministic checksum, which isolates comparison lookup and regression-gating work for same-workload A/B proof when later report-schema extraction or lookup-table changes touch baseline comparison. Baseline comparison now tries the same-order helper before the small-baseline lookup when current and baseline have equal case counts, because the same-workload `ci-current.json` versus itself bench proved lower comparison cost while preserving counts and checksum. Unequal small baselines still scan directly when the baseline has 32 or fewer cases, because the same-workload comparison bench proved that avoiding lookup-table allocation and tree construction was faster for the current `latest.json` versus `ci-current.json` pair while preserving counts and checksum; larger baselines with the same ordered case IDs compare zipped rows in one validation/comparison pass because same-workload proof showed that avoiding both the lookup table and duplicate ID walk is faster for same-shape reports, unchanged same-order rows skip regression-envelope calculation when medians are equal because identical-report proof showed a lower comparison cost with unchanged counts and checksum, and other larger baselines keep the existing `BTreeMap` lookup path. The comparison output vectors for missing-baseline IDs and improvements are sized from current/baseline case counts because the same workload proved this avoids repeated vector growth and lowers comparison cost while preserving output ordering, counts, and checksum. The missing-baseline vector now reserves up to the current case count on lookup paths because the same workload proved that avoiding the last under-reserve growth lowers comparison cost while preserving counts and checksum; same-order comparisons skip that reservation because aligned case IDs prove no baseline rows can be missing. Lookup comparisons now record strict improvements before computing the regression envelope because the current `latest.json` versus `ci-current.json` workload proved that skipping variance math for 25 improvement rows lowers comparison cost with unchanged counts and checksum. The non-default frame-pacing metrics harness repeatedly inserts missed-frame and hitch metric rows for a fixed representative frame interval slice, reports metric count and checksum, and isolates `insert_frame_pacing_metrics` changes from full suite runtime. Frame-pacing metrics now insert the standard 60 Hz and 120 Hz rows with static metric key strings because same-workload proof showed this avoids repeated key formatting while preserving metric count and checksum; nonstandard refresh tiers keep the formatted fallback. The non-default case-filter harness repeatedly checks representative case IDs and family prefixes under the current `OXIDE_PERF_RUNNER_FILTER` value, which isolates case-selection plumbing for same-workload A/B proof when filter parsing or manifest-driven case selection changes. Case filters are parsed once per process and reused across family-prefix and case checks, because the same case-filter workload proved this removes repeated environment lookup, split, trim, string allocation, and vector construction while preserving representative filter counts and checksum. Case filter matching also relies on `starts_with` for exact case IDs because exact strings satisfy that prefix predicate; removing the separate equality branch proved a smaller same-workload win with the same counts and checksum. Markdown summary/header lines stream directly into the report body instead of allocating formatted strings for suite, label, coverage, and comparison lines, because the 65,536-iteration same-workload report render A/B showed the direct path lowered long-run render cost while preserving output bytes. Markdown comparison regression and missing-baseline lines also stream directly into the report body, because the comparison-aware same-workload render A/B proved lower report-generation cost while preserving output bytes and comparison counts. Markdown case metric summaries stream directly instead of first allocating per-metric formatted strings and joining them, because the same workload proved lower render cost while preserving output bytes. Their fallback duplicate suppression uses a direct priority-index lookup instead of scanning all priority names for each metric key, because the current workspace report renders 869 metric keys and same-workload proof showed the direct lookup lowered render cost while preserving output bytes. Markdown result rows also stream into the report body directly instead of building one intermediate row string per case, and metric summaries now stream inline into those rows instead of allocating a separate summary string, because the same workload proved both changes as byte-identical report-generation wins. Contract coverage rows now stream their cells and note separators directly into the report body instead of formatting an intermediate row string and joining notes, because the same workload proved lower render cost with identical output bytes. Exercised contract note, finding, and baseline footer lines also stream directly so the current workspace report avoids temporary formatted strings for those tail sections while preserving byte-identical Markdown. Persisted report schema tests now freeze workspace, Oxide-device, UIKit-device, and browser WebGPU root keys, case-row shapes, case counts, compact case-id digests, nested coverage/contract/finding keys, UIKit and Oxide-device metric key classes, browser WebGPU case key classes, high-value browser WebGPU section key sets, and native workspace renderer metric key sets before later schema extraction work moves those definitions into generated manifests. The separate browser WebGPU baseline under `benchmarks/web/latest.json` is guarded by report-shape tests so the 17-row default browser matrix cannot drop browser startup timing and static package bytes, latency distributions, 60 Hz/120 Hz missed-frame and hitch metrics, current upload summaries with direct glyph/RGBA timestamp totals, current clean-layer cache-hit/skipped-draw counters, retained summaries, explicit backend-path coverage rows tying every important default WebGPU path family to distributions and explanatory counters, report-level and per-row warm-resource-churn zero-growth summaries for current warm rows, family-level GPU resource attribution, family-level scratch attribution, current-row Rust/WASM allocation counters with bounded per-frame budgets and zero reallocations, frame-loop allocation stage attribution, renderer submit sub-stage allocation attribution, zero WASM memory growth plus Chrome JS heap sampling across benchmark marks after prewarm, Chrome browser trace event counts, benchmark User Timing labels, per-benchmark trace intervals with scoped event/GPU/WebGPU counts from a duplicate benchmark-report run, resource lifetime counters, timestamp-query attribution status, GPU timestamp stage-breakdown totals reconciled to every source row, current backdrop-batch copy/pass counters, current neon-marker marker/state counters, current direct-surface one-pass counters, or pixel checks while still looking like a valid persisted browser report.
The non-default distribution metrics harness repeatedly inserts representative `frame_ms`, `event_to_visible_ms`, and `gpu_ms` distribution rows, reports metric count and checksum, and isolates distribution metric-key insertion changes from full suite runtime. Distribution metrics now insert those standard rows with static metric key strings because same-workload proof showed this avoids repeated key formatting while preserving metric count and checksum; uncommon prefixes keep the formatted fallback. Distribution metric rows now use a distribution-only summary because same-workload proof showed this skips unused min and mean work while preserving p50/p95/p99/peak values and checksum, common short sample slices sort in a fixed stack buffer because same-workload proof showed this avoids heap vector allocation while preserving stable sort semantics, and fixed 24-sample summaries use direct interpolation because same-workload proof showed this avoids generic quantile calls while preserving metric count and checksum. The non-default case metric contract harness repeatedly validates an existing report's required frame/GPU metric keys, reports case/required counts plus checksum, and isolates required-key validation changes from full suite runtime. Case metric contract validation now checks static required-key arrays for frame and GPU rows because same-workload proof showed this avoids repeated validation-key formatting while preserving required counts and checksum. The non-default contract coverage harness repeatedly validates an existing report's contract coverage rows, reports layer/battery/note counts plus checksum, and isolates contract coverage validation changes from full suite runtime. Contract coverage gap-note validation now uses an allocation-free ASCII phrase scan because same-workload proof showed this avoids per-note lower-case string allocation while preserving layer/battery/note counts and checksum. Its phrase dispatch uses a first-byte ASCII fold before the existing prefix checks because same-workload proof showed this avoids the per-byte uppercase branch while preserving the same forbidden phrase set. Contract coverage phrase matching now uses a tail-only ASCII phrase loop after the first-byte dispatch because same-workload proof showed this avoids redundant first-byte comparison while preserving the forbidden phrase set.
The committed workspace baseline is also guarded for the macOS Metal animation refresh-matrix and collection-navigation frame-pacing rows, so local animation hitch proof cannot silently degrade into CPU-only or metadata-only coverage.
Retained layout coverage is guarded at the persisted-report level as well: clean retained encodes must keep full reuse counters, dirty-leaf encodes must keep node reuse/rebuild counters, dirty-class layout rows must keep zero-layout counters, and non-draw dirty rows must prove retained reuse without draw rebuilds.
Collection and text coverage are guarded at the persisted-report level too: collection rows must preserve keyed-index and dirty-prefix A/B diagnostics, and text rows must preserve shaped-run cache, dirty atlas upload, atlas pressure, fallback-font, bidi, shaped cursor-map diagnostics, and explicit absence of retired text audit rows in the refreshed workspace latest baseline.
Contract coverage battery entries share one status-and-note helper so each required case set is evaluated once, then rendered consistently into the persisted report. Newly generated native reports keep only the gated wrapped-label and picker text current rows after the slower legacy text audit rows were retired with same-workload A/B proof.

### Schema versioning rules

Persisted report and evidence schemas are part of the performance contract because A/B claims are only reviewable when old and new reports mean the same thing. The following changes require an intentional version bump or an explicit hard-cutover note in the same reviewed change:

- `PerfReport.version`, browser WebGPU report `version`, Oxide-device report `version`, and UIKit-device report `version` must bump when a top-level JSON object adds, removes, renames, or changes the meaning of a required root key.
- Common case-row fields must bump the owning report version when a required key is added, removed, renamed, moved between root/case/metrics, changes unit semantics, or changes aggregation semantics.
- Benchmark IDs are semantic workload identifiers. Reusing an existing ID for a different workload is forbidden; change the ID instead. Adding or retiring IDs requires refreshed persisted baselines, case-id digest updates, mapped docs, and same-workload A/B proof for any cleanup/deletion, but does not require a JSON schema version bump when the row shape and field meanings stay unchanged.
- Metric keys inside `metrics` may grow compatibly only when old reports remain readable and the metric meaning is additive. Renaming, deleting, reuniting, moving, or redefining a metric key requires a report version bump, regenerated baselines, metric-key freeze updates, and docs that name the migration.
- Browser WebGPU summary sections, browser startup/package evidence, backend-path coverage rows, benchmark marks, allocation-stage objects, warm-resource-churn objects, timestamp attribution sections, and pixel-check objects follow the same rule: additive compatible keys update freeze digests and docs; removals, renames, unit changes, or changed reconciliation semantics bump the browser report version.
- Oxide-device and UIKit-device rows must bump their report version when metric key classes, style/cache/refresh metadata, energy/GPU fields, or contract battery row shapes change in a way that old and new reports cannot be compared under the same meaning.
- Native ABI evidence layouts use ABI struct version/size fields rather than the report JSON version. Any layout, alignment, field order, ownership, or callback-payload semantic change must bump the relevant ABI/version header or size contract, update Rust and host-side layout assertions, and document how existing device evidence should be interpreted.
- Legacy-row retirement is a benchmark matrix hard cutover, not a rollback archive. A loser row, comparison-only metric, env switch, or loser-only test may be deleted only after same-workload A/B evidence proves the retained path is faster, the refreshed latest baseline omits the retired row, negative tests prove it stays retired, and docs name the decision. If retirement also changes root/case shape or metric semantics, bump the report version too.
- Historical dated reports and CI snapshots may remain readable under their original version. They must not be silently edited to masquerade as a newer schema; any regenerated artifact must state the new generated label/date and satisfy the current freeze tests.

## Preconditions and postconditions

- Preconditions:
  - The workspace must build with the relevant crates and test scenes available.
  - Benchmark cases must record at least one sample.
  - Persisted baselines must be comparable to the current report schema when regression checks are requested.
- Postconditions:
  - A successful suite run produces a complete `PerfReport` with coverage accounting.
  - Baseline comparisons either report no gated regressions or fail with explicit mismatches.
- Invariants maintained:
  - Every persisted case carries contract metadata alongside latency distributions.
  - Frame rows cannot pass the full-suite contract without frame distribution and pacing metrics.
  - GPU frame rows cannot pass the full-suite contract without direct GPU timing distributions.
  - The persisted macOS Metal animation and collection-navigation frame-pacing rows cannot pass report tests without direct GPU timing, frame timing, 60 Hz and 120 Hz missed-frame/hitch metrics, and `oxide-metal` refresh-mode metadata.
  - Retained authoring and dirty-class layout rows cannot pass report tests without retained reuse/rebuild counters and explicit zero-layout evidence for non-layout dirtiness.
  - Collection/text rows cannot pass report tests without the diagnostics that distinguish indexed versus scan reconciliation, incremental versus full prefix repair, cached text shaping/upload, atlas evictions, atlas dirty updates, fallback fonts, bidi boundaries, cursor maps, and absence of retired text audit rows.
  - Browser WebGPU report rows cannot pass the report tests without browser startup timing and package byte evidence, p50/p95/p99/peak, missed-frame, hitch, pixel-check, draw-family counters, layer cache counters, pass-family counters that sum to total render passes, resource-lifetime counters, image-upload temp/scratch counters, current upload fields plus direct glyph/RGBA timestamp totals, current effect-uniform counters plus direct GPU timestamp totals, current retained clean-layer summary fields, current direct-surface one-pass/timestamp fields, explicit backend-path coverage rows tying every important default WebGPU path family to distributions and explanatory counters, report-level and per-row warm-resource-churn zero-growth summaries including family-level GPU resource and CPU scratch growth totals, current-row Rust/WASM allocation counters with bounded per-frame budgets and zero reallocations, frame-loop allocation stage attribution with no unattributed allocations, renderer submit sub-stage allocation attribution with no unattributed parent-submit allocations, zero WASM memory growth across benchmark marks after prewarm, Chrome JS heap sampling/GC support and finite heap growth fields for each benchmark mark, Chrome browser trace event counts with `capture_phase=benchmark-report` and `timing_source=untraced-baseline-report`, traced browser User Timing labels and positive trace intervals for every benchmark family, timestamp-query attribution status, GPU timestamp stage breakdown totals reconciled to every row, and collected timestamp pass counts that match render-pass counts when timestamp queries are supported.
  - Coverage counts and covered-name inventories stay synchronized with the registered case inventory.
  - Persisted report case-id sets cannot change without updating the corresponding count and digest gate.
  - Persisted nested report section keys, metric key classes, and web case key classes cannot change without updating the corresponding count and digest gate.
  - Persisted native renderer metric key sets cannot change without updating the corresponding count and digest gate.
  - Missing metrics default safely through serde so older baselines remain readable while the schema grows.

## Changelog
- 2026-07-15: registered C60 image-store 100/1,000/10,000 architecture rows and the 1,000-icon public authoring journey.
- 2026-07-13: registered C27 retained-snapshot spatial-query and Metal spatial-damage authoring contracts.
- 2026-07-13: registered the public-authoring C26 300-node dynamic property animation contract.
- 2026-07-13: Added public retained-snapshot Metal authoring coverage backed by the C24 clean prepared-chunk contract.
- 2026-07-13: Added C23 retained cache-pressure engine rows and public `UiSurface` cache-policy authoring coverage.

- 2026-07-13: routed visible scene/journey Metal rows through three-slot frame resources and added depth, ring-byte, growth, and backpressure metrics while preserving separate eight-slot architecture stress.
- 2026-07-12: Added 132 C01 CPU/Metal rendering-architecture rows plus the opt-in ten-row WebGPU primitive matrix; isolated cache/feature variants, direct GPU distributions, workload counters, and queue/RAF pacing are documented in `architecture_matrix.md`.
- 2026-07-12: Bumped generated browser WebGPU reports to version 6 for the C00 hard cutover from synchronous batch pacing claims to distinct CPU-submit throughput and raw one-submit-per-RAF distributions with environment, stage, GPU, queue-drain, and instrumentation-overhead evidence; historical version 5 baselines remain unchanged until C62 promotion.
- 2026-06-22: Used direct interpolation for fixed 6-, 10-, 12-, and 24-sample suite summaries after same-workload A/B proof lowered summary cost from `0.218`/`0.192 us_per_iter` to `0.187`/`0.186 us_per_iter` with unchanged group count, summary count, and checksum.
- 2026-06-22: Sorted common suite sample summaries in a fixed stack buffer after same-workload A/B proof lowered summary cost from `0.223`/`0.221 us_per_iter` to `0.220`/`0.214`/`0.195 us_per_iter` with unchanged group count, summary count, and checksum.
- 2026-06-22: Used direct interpolation for fixed 24-sample distribution metric summaries after same-workload A/B proof lowered insertion cost from `0.894`/`0.906 us_per_iter` to `0.876`/`0.877 us_per_iter` with unchanged metric count and checksum.
- 2026-06-22: Sorted common short distribution metric samples in a fixed stack buffer after same-workload A/B proof lowered insertion cost from `0.888`/`0.900 us_per_iter` to `0.881`/`0.868 us_per_iter` with unchanged metric count and checksum.
- 2026-06-22: Summarized distribution metric rows with a distribution-only summary after same-workload A/B proof lowered insertion cost from `0.880`/`0.861 us_per_iter` to `0.859`/`0.846 us_per_iter` with unchanged metric count and checksum.
- 2026-06-22: Matched contract coverage gap-note phrase tails after same-workload A/B proof lowered validation cost from `2.119`/`2.100 us_per_iter` to `1.869`/`1.852 us_per_iter` with unchanged layer/battery/note counts and checksum.
- 2026-06-22: Dispatched contract coverage gap-note checks with a first-byte ASCII fold after same-workload A/B proof lowered validation cost from `2.436`/`2.450 us_per_iter` to `1.990`/`1.965 us_per_iter` with unchanged layer/battery/note counts and checksum.
- 2026-06-22: Validated contract coverage gap notes with an allocation-free ASCII phrase scan after same-workload A/B proof lowered validation cost from `4.311`/`4.292 us_per_iter` to `2.465`/`2.441 us_per_iter` with unchanged layer/battery/note counts and checksum.
- 2026-06-22: Added a non-default contract coverage validation harness for same-workload gap-note validation A/B tests.
- 2026-06-22: Validated case metric contracts with static required-key arrays after same-workload A/B proof lowered validation cost from `19.370`/`19.371 us_per_iter` to `7.539`/`7.570 us_per_iter` with unchanged case/required counts and checksum.
- 2026-06-22: Added a non-default case metric contract validation harness for same-workload required-key validation A/B tests.
- 2026-06-22: Inserted standard `frame_ms`, `gpu_ms`, and `event_to_visible_ms` distribution metrics with static key strings after same-workload A/B proof lowered insertion cost from `1.309`/`1.306 us_per_iter` to `0.880`/`0.871 us_per_iter` with unchanged metric count and checksum.
- 2026-06-22: Added a non-default distribution metrics insertion harness for same-workload distribution metric-key A/B tests.
- 2026-06-22: Inserted standard 60 Hz and 120 Hz frame-pacing metrics with static key strings after same-workload A/B proof lowered insertion cost from `0.889`/`0.915 us_per_iter` to `0.763`/`0.753 us_per_iter` with unchanged metric count and checksum.
- 2026-06-22: Added a non-default frame-pacing metrics insertion harness for same-workload missed-frame and hitch metric A/B tests.
- 2026-06-22: Recorded lookup-path improvements before regression-envelope calculation after same-workload A/B proof lowered `latest.json` versus `ci-current.json` comparison cost from `11.836`/`11.839 us_per_iter` to `11.817`/`11.804 us_per_iter` with unchanged counts and checksum.
- 2026-06-22: Tried the same-order `compare_reports` helper before small-baseline lookup for equal-length reports after same-workload A/B proof lowered `ci-current.json` versus itself comparison cost from `0.429`/`0.369 us_per_iter` to `0.190`/`0.170 us_per_iter` with unchanged counts and checksum.
- 2026-06-22: Added an equal-median fast path to the same-order `compare_reports` helper after same-workload A/B proof lowered `latest.json` versus itself comparison cost from `0.608`/`0.580 us_per_iter` to `0.516`/`0.492 us_per_iter` with unchanged counts and checksum.
- 2026-06-22: Rejected lazy same-order improvements allocation because the same-workload `latest.json` versus itself A/B did not beat the retained same-order improvements preallocation (`0.603 us_per_iter` versus the faster A-side `0.596 us_per_iter`) with unchanged counts and checksum.
- 2026-06-22: Refined the same-order `compare_reports` fast path to validate and compare in one pass after same-workload A/B proof lowered `latest.json` versus itself comparison cost from `0.686`/`0.658 us_per_iter` to `0.615`/`0.584 us_per_iter` with unchanged counts and checksum.
- 2026-06-22: Added a same-order large-baseline fast path to `compare_reports` after same-workload A/B proof lowered `latest.json` versus itself comparison cost from `7.483`/`7.449 us_per_iter` to `0.694`/`0.660 us_per_iter` with unchanged counts and checksum.
- 2026-06-22: Added the non-default JSON String render harness and recorded that routing it through the pre-sized byte serializer was rejected because the same-workload A/B regressed.
- 2026-06-22: Retired the default browser WebGPU effect-uniform legacy row after same-workload A/B proof and updated the report gate to current-only effect-uniform coverage in the 20-row browser matrix.
- 2026-06-22: Retired the default browser WebGPU neon-marker legacy-rebind row after same-workload A/B proof and updated the report gate to current-only neon-marker coverage in the 18-row browser matrix.
- 2026-06-22: Retired the default browser WebGPU clean-layer dirty rerender row after same-workload A/B proof and updated the report gate to current-only clean-layer coverage in the 17-row browser matrix.
- 2026-06-22: Retired the default browser WebGPU direct-surface forced-scene-present row after same-workload A/B proof and updated the report gate to current-only direct-surface coverage in the 19-row browser matrix.
- 2026-06-22: Added a non-default JSON render measurement harness for same-workload pretty JSON serialization A/B tests.
- 2026-06-22: Pre-sized persisted `PerfReport` JSON serialization after same-workload A/B proof lowered the 65,536-iteration JSON render cost from `152.833`/`152.066 us_per_iter` to `151.467`/`151.210 us_per_iter` with identical `158970` bytes per report.
- 2026-06-22: Tightened the persisted `PerfReport` JSON serializer capacity hint after same-workload A/B proof lowered the 65,536-iteration JSON render cost from `151.329`/`151.194 us_per_iter` to `150.422`/`150.060 us_per_iter` with identical `158970` bytes per report.
- 2026-06-22: Rendered latest-plus-dated Markdown baseline outputs from one body after same-workload A/B proof lowered write-path render cost from `245.139`/`250.237 us_per_iter` to `125.709`/`125.452 us_per_iter` with unchanged output bytes and comparison counts.
- 2026-06-22: Retired the default browser WebGPU layer-effects legacy row after same-workload A/B proof and updated the report gate to the 22-row current-only layer-effects matrix.
- 2026-06-22: Retired the default browser WebGPU command-family legacy row after same-workload A/B proof and updated the report gate to the 23-row current-only command-family matrix.
- 2026-06-22: Retired the default browser WebGPU upload legacy rows and upload A/B export after same-workload A/B proof and updated the report gate to version 5 with current-only upload rows.
- 2026-06-22: Retired the default browser WebGPU backdrop-batch per-copy row after same-workload A/B proof and updated the report gate to the current-only backdrop matrix.
- 2026-06-22: Sized `compare_reports` output vectors for missing-baseline IDs and improvements after same-workload A/B proof lowered `latest.json` versus `ci-current.json` comparison cost from `12.144`/`12.100 us_per_iter` to `11.802`/`11.779 us_per_iter` with unchanged comparison counts and checksum.
- 2026-06-22: Reserved the `compare_reports` missing-baseline output vector to the current case count after same-workload A/B proof lowered `latest.json` versus `ci-current.json` comparison cost from `11.472`/`11.432 us_per_iter` to `11.421`/`11.356 us_per_iter` with unchanged comparison counts and checksum.
- 2026-06-22: Added a small-baseline linear scan path to `compare_reports` after same-workload A/B proof lowered `latest.json` versus `ci-current.json` comparison cost from `13.579`/`13.467 us_per_iter` to `12.130`/`12.150 us_per_iter` with unchanged comparison counts and checksum.
- 2026-06-22: Added the non-default compare-reports bench harness and rejected replacing the comparison baseline `BTreeMap` with `HashMap` after same-workload A/B regressed from `13.320`/`13.342 us_per_iter` to `14.727`/`14.730 us_per_iter` with unchanged comparison counts and checksum.
- 2026-06-22: Added the non-default case-filter bench harness and cached parsed perf-runner case filters after same-workload A/B proof lowered representative filter-check cost from `3.432`/`3.365 us_per_iter` to `0.233`/`0.233 us_per_iter` with unchanged counts and checksum.
- 2026-06-22: Removed the redundant exact equality branch from perf-runner case filtering after same-workload A/B proof lowered the cached representative filter-check cost from `0.224`/`0.224 us_per_iter` to `0.216`/`0.216 us_per_iter` with unchanged counts and checksum.
- 2026-06-22: Added comparison-aware Markdown render bench coverage and streamed comparison lines directly after same-workload A/B proof lowered `latest.json` versus `ci-current.json` render cost from `125.236`/`124.734 us_per_iter` to `119.716`/`119.505 us_per_iter` with unchanged output bytes and comparison counts.
- 2026-06-22: Replaced Markdown metric-summary fallback priority scans with direct priority-index lookup after same-workload A/B proof lowered 65,536-iteration report render cost from rebuilt A `114.411 us_per_iter` to rebuilt B `112.988` and `113.451 us_per_iter` with identical `43547` bytes per report.
- 2026-06-22: Streamed Markdown contract note, finding, and baseline footer lines directly after same-workload A/B proof lowered 65,536-iteration report render cost from rebuilt A `116.961 us_per_iter` to rebuilt B `115.823` and `115.822 us_per_iter` with identical `43547` bytes per report.
- 2026-06-22: Streamed Markdown summary/header lines directly after same-workload A/B proof lowered 65,536-iteration report render cost from rebuilt A `117.639 us_per_iter` to rebuilt B `116.537` and `116.556 us_per_iter` with identical `43547` bytes per report.
- 2026-06-22: Streamed Markdown contract coverage rows directly after same-workload A/B proof lowered 65,536-iteration report render cost from rebuilt A `119.769 us_per_iter` to rebuilt B `118.592` and `117.077 us_per_iter` with identical `43547` bytes per report.
- 2026-06-22: Streamed Markdown metric summaries inline into result rows after same-workload A/B proof lowered 65,536-iteration report render cost from rebuilt A `124.487 us_per_iter` to rebuilt B `119.396 us_per_iter` with identical `43547` bytes per report.
- 2026-06-22: Streamed Markdown result rows directly after same-workload A/B proof lowered 65,536-iteration report render cost from rebuilt A `137.006 us_per_iter` to rebuilt B `127.069 us_per_iter` with identical `43547` bytes per report.
- 2026-06-22: Streamed Markdown case metric summaries directly after same-workload A/B proof lowered 65,536-iteration report render cost from rebuilt A `172.635 us_per_iter` to rebuilt B `139.736 us_per_iter` with identical `43547` bytes per report.
- 2026-06-22: Added a non-default Markdown render measurement harness for same-workload report-generation A/B tests.
- 2026-06-22: Added browser WebGPU startup timing and package-size report evidence, bumping the browser report schema to version 3.
- 2026-06-22: Retired default browser WebGPU upload-scratch report rows after same-workload A/B proof and updated the report gate to the 29-row matrix.
- 2026-06-22: Retired the default browser WebGPU glyph-run legacy row after same-workload A/B proof and updated the report gate to the 27-row current-only glyph-run matrix.
- 2026-06-22: Retired the default browser WebGPU ID-mask legacy row after same-workload A/B proof and updated the report gate to version 4 with a 28-row matrix.
- 2026-06-22: Added nested persisted report key freezes for coverage, contract, findings, device metric key classes, browser summary sections, and browser case key classes.
- 2026-06-22: Added explicit persisted schema-versioning rules and a docs-content guard for benchmark/report evidence changes.
- 2026-06-22: Added native workspace renderer metric-key freezes for GPU scene, id-mask, animation, navigation, Scene3D, and image first-visible rows.
- 2026-06-22: Added browser WebGPU report sub-object key freezes for high-value A/B evidence sections.
- 2026-06-22: Added persisted report root/case schema and case-id digest freeze tests for workspace, Oxide-device, UIKit-device, and browser WebGPU baselines, refreshed the workspace latest report to 161 current rows, and updated the browser WebGPU report gate after retiring standalone cache A/B rows.
- 2026-06-02: Expanded the browser WebGPU report gate to enforce the 37-row matrix with draw-item coalescing current-versus-uncoalesced A/B coverage.
- 2026-06-01: Added per-benchmark browser User Timing mark gates to the WebGPU report and duplicate Chrome trace capture.
- 2026-06-02: Added GPU timestamp stage-breakdown reconciliation gates to the browser WebGPU report.
- 2026-06-02: Added per-benchmark Chrome trace interval gates to the WebGPU report.
- 2026-06-02: Added direct glyph/RGBA timestamp-total fields to the browser WebGPU upload A/B summary gate.
- 2026-06-02: Added browser WebGPU Rust/WASM frame allocation audit counters and budget gates.
- 2026-06-02: Added browser WebGPU submit sub-stage WASM allocation attribution gates.
- 2026-06-02: Added browser WebGPU frame-loop allocation stage attribution gates.
- 2026-06-02: Added neon-marker current-versus-legacy A/B coverage to the browser WebGPU report gate.
- 2026-06-02: Expanded the browser WebGPU report gate with retained clean-layer comparison coverage before the dirty row was later retired.
- 2026-06-02: Expanded the browser WebGPU report gate to enforce the 33-row matrix with glyph-run current-versus-legacy-rebind A/B coverage.
- 2026-06-02: Added direct-surface current-versus-forced-scene-present A/B coverage to the browser WebGPU report gate.
- 2026-06-02: Expanded the browser WebGPU report gate with command-family current-versus-legacy A/B coverage.
- 2026-06-02: Expanded the browser WebGPU report gate to enforce the 26-row matrix with layer/damage/effects current-versus-legacy A/B coverage.
- 2026-06-22: Updated the browser WebGPU report gate to enforce the current-only mixed-scene row after same-workload A/B proof retired the slower default legacy row.
- 2026-06-02: Expanded the browser WebGPU report gate to enforce the 25-row matrix with mixed-scene current-versus-legacy A/B coverage.
- 2026-06-01: Added direct GPU timestamp-total fields to the browser WebGPU effect-uniform A/B summary.
- 2026-06-01: Added the browser WebGPU Chrome trace summary gate and tied it to duplicate benchmark-report trace capture.
- 2026-06-01: Added the browser WebGPU warm-resource-churn summary gate for current warm rows.
- 2026-06-01: Expanded the browser WebGPU report gate to enforce the 24-row matrix with backdrop-batch A/B coverage and texture-copy/render-pass reductions.
- 2026-06-01: Expanded the browser WebGPU report gate to enforce the 20-row matrix with effect-uniform A/B coverage and effect-uniform counters.
- 2026-06-01: Expanded the browser WebGPU report gate to enforce the 22-row matrix with clip-state cache A/B coverage and clip-depth/scissor counters.
- 2026-06-01: Expanded the browser WebGPU report gate to enforce the 18-row matrix with upload-scratch A/B coverage and image-upload temp/scratch counters.
- 2026-06-02: Added browser WebGPU warm scratch family attribution gates for draw, Scene3D, effect, ID-mask, image-upload, and resource-table growth.
- 2026-06-02: Added browser WebGPU GPU resource family attribution gates for draw, image, target, Scene3D, effect, and ID-mask resource churn.
- 2026-06-01: Expanded the browser WebGPU report gate to enforce the 16-row matrix with draw-state cache A/B coverage.
- 2026-06-01: Expanded the browser WebGPU report gate to enforce the 14-row matrix with generic command-family coverage.
- 2026-06-01: Expanded the browser WebGPU report gate with dedicated layer/damage/effects coverage.
- 2026-06-01: Added browser WebGPU timestamp-query collection gates and per-row timestamp pass count checks.
- 2026-06-01: Expanded the browser WebGPU report gate with resource/explanatory counters.
- 2026-06-01: Added browser WebGPU pass-family attribution counters and timestamp-query capability status to the persisted web report gate.
- 2026-06-01: Added cursor-map structural metrics to text-input cursor-pick rows: cursor count, byte boundaries, boundary checksum, affinity splits, width span, and fallback shape-run count.
- 2026-06-01: Added `cpu.authoring.collection_measure_cache.bounded_churn` so bounded variable collection measurement-cache repair after large key churn is represented in workspace baselines.
- 2026-06-01: Added persisted eviction, dirty-rect, resident-glyph, revision, vertex, and index metrics to `cpu.system.text_atlas_pressure`.
- 2026-06-01: Added persisted workspace report gates for collection keyed/prefix A/B rows and text cache/atlas/cursor rows.
- 2026-06-01: Added a persisted workspace report gate for retained-surface authoring and dirty-class layout reuse rows.
- 2026-06-01: Added a persisted workspace report gate for macOS Metal animation/effects and collection-navigation frame-pacing rows.
- 2026-06-01: Added a persisted WebGPU report contract test for browser frame distributions, missed-frame/hitch metrics, pixel checks, and ID-mask current-vs-legacy A/B fields.
- 2026-06-01: Added a focused report test for the state-reconciliation battery so single-node, 1 percent, 10 percent, and full-theme mutation rows stay gated.
- 2026-06-01: Bounded `CollectionView` variable measurement cache retention while keeping collection key-reconcile and prefix-repair A/B rows as the workspace guard.
- 2026-06-01: Added `cpu.system.wrapped_label_cached_encode` and a legacy audit row for ASCII wrapped-label cache-miss fitting; the slower legacy row was later retired from newly generated native reports after same-workload A/B proof.
- 2026-05-31: Added `gpu.journey.collection_navigation.frame_pacing` so a real collection navigation flow persists macOS Metal event-to-visible, frame, GPU, missed-frame, and hitch distributions.
- 2026-05-31: Added `cpu.layout.scoped_tree_mutation.add_remove` so scoped surface add/remove mutations are measured with layout skip and retained sibling replay counters.
- 2026-05-31: Broadened the case metric contract so all frame rows require frame distribution plus missed-frame/hitch metrics, and GPU frame rows require GPU distribution metrics.
- 2026-05-31: Updated the cursor-pick cluster-map note to reflect `oxide_text::ShapedCursorMap` as the text-input backing artifact.
- 2026-05-31: Added `cpu.text_input.cursor_pick.mixed_bidi_affinity` so mixed LTR/RTL upstream/downstream cursor-map positions are measured beside LTR, RTL, and fallback-font cursor picking.
- 2026-05-31: Added `cpu.system.text_fallback_label_encode` so visible fallback-font glyph encoding is measured beside cursor fallback.
- 2026-05-31: Added `cpu.text_input.cursor_pick.fallback_cluster_map` so configured fallback-font cursor widths are measured beside the LTR and RTL cursor-pick cases.
- 2026-05-31: Added `cpu.text_input.cursor_pick.rtl_cluster_map` so descending RTL cursor-map picking is measured beside the LTR Unicode cursor-pick case.
- 2026-05-31: Added `cpu.layout.non_draw_dirty.retained_reuse` so accessibility/hit-test-only dirty classes are measured and gated with zero layout work plus retained draw-list reuse counters.
- 2026-05-31: Added `cpu.layout.node_content_dirty.retained_replay` so node-scoped text/image/camera dirty classes are measured and gated with zero layout work plus retained subtree replay counters.
- 2026-05-31: Added `cpu.system.text_atlas_dirty_rect_upload` so incremental text atlas dirty-rect publication is persisted with create/update counts and upload-area ratios.
- 2026-05-31: Added `gpu.animation.effects.refresh_matrix` so animation/effects frame pacing has a dedicated Metal row with direct GPU and 60/120 Hz hitch metrics.
- 2026-05-31: Updated `cpu.authoring.drawlist_text_replay.multi_atlas` to exercise the checked retained draw-list replay API rather than raw cached append.
- 2026-05-31: Collection scroll journey rows now persist item-revision query totals and revision-hint state for variable prefix reuse.
- 2026-05-31: Added `cpu.layout.paint_only.opacity_clip` so opacity/clip dirty-class edits are measured and gated with zero layout work plus retained subtree replay counters.
- 2026-05-31: Added `cpu.layout.transform_only.reposition` so transform-only dirty-class edits are measured and gated with zero layout work.
- 2026-05-31: Added `cpu.layout.descendant_only.incremental_relayout` so parent measurement avoidance for fixed-outer internal subtree edits is represented in workspace baselines.
- 2026-05-31: Added `cpu.text_input.cursor_pick.cluster_map` so long-line Unicode pointer-to-cursor mapping remains in the text-input perf contract.
- 2026-06-01: Added persisted text-byte, prefix-boundary, width-entry, and shaped-run metrics to `cpu.system.text_prefix_width_map`.
- 2026-05-31: Added `cpu.system.text_prefix_width_map` so one-shaped-run caret prefix maps are represented in workspace baselines.
- 2026-05-31: Added `cpu.layout.dirty_subtree.incremental_relayout` so per-node layout dirtiness and clean-subtree skips are represented in workspace baselines.
- 2026-05-31: Added `cpu.authoring.drawlist_text_replay.multi_atlas` for the public multi-atlas cached text draw-list replay API.
- 2026-05-31: Updated `cpu.authoring.surface_router.compose` to persist retained current/overlay/popup composition reuse metrics.
- 2026-05-31: Added `cpu.authoring.collection_key_reconcile.indexed` and `cpu.authoring.collection_key_reconcile.scan` so the public collection key-index hook has persisted A/B coverage.
- 2026-05-31: Added `cpu.authoring.collection_prefix_update.incremental` and `cpu.authoring.collection_prefix_update.full_scan` so dirty-range variable prefix repair has persisted A/B coverage.
- 2026-05-31: Added `cpu.authoring.surface_retained.text_atlas_context` for retained surface replay with live `TextCtx` text-atlas revision context.
- 2026-05-31: Added `cpu.text_input.ime.composition_commit_cycle` and `cpu.journey.text_ime_composition_cycle` so IME marked-text, commit, selection, keyboard geometry, and routed redraw paths are represented in persisted workspace reports.
- 2026-05-31: Added `cpu.authoring.surface_retained.dirty_leaf_encode` so paint-only dirty-leaf retained surface reuse is measured with retained-node reuse/rebuild counters.
- 2026-05-31: Added `cpu.system.text_atlas_pressure` so constrained glyph-atlas eviction is measured in the workspace suite.
- 2026-05-14: Added `cpu.bridge.web_backend_surface` and refreshed the workspace baseline to keep the WebAssembly backend represented in the Rust-side performance contract.
- 2026-04-30: Removed the one-use perf-filter activity wrapper; suite coverage gating now checks the parsed filter list directly.
- 2026-04-18: Collapsed duplicated contract-battery status and note conditionals into shared helper logic while preserving report output semantics.
- 2026-04-14: Collapsed repetitive coverage assertions into one shared check table while preserving the same incomplete-family error messages.
- 2026-04-11: Removed redundant manual `Default` implementations for internal CLI options and `PerfCaseResult`; derived defaults preserve the same serde fallback behavior with less implementation surface.
- 2026-04-23: Added authoring coverage for mixed retained `scene3d` plus 2D Metal frames.
