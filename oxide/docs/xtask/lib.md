# `xtask`

`xtask` retains the repository experiment-manifest check, iOS preparation,
Oxide device-performance capture, time-profiler summary, and the aggregate
test command. The prior cross-framework comparison campaign and legacy
UIKit/React device-performance commands have been removed.
