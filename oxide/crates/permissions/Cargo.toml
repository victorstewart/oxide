[package]
name = "oxide-permissions"
version = "0.1.0"
edition = "2021"
license = "MIT OR Apache-2.0"

[dependencies]
oxide-platform-api = { path = "../platform-api" }
parking_lot = "0.12"

[features]
default = []

[lib]
name = "oxide_permissions"
path = "src/lib.rs"
crate-type = ["rlib"]
