[package]
name = "oxide-starscape-background"
version = "0.1.0"
edition = "2021"
license = "MIT OR Apache-2.0"
description = "Renderer-agnostic starscape background drawing for Oxide"

[lib]
path = "src/lib.rs"

[dependencies]
oxide-renderer-api = { path = "../renderer-api" }
