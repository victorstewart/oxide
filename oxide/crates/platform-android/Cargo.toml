[package]
name = "oxide-platform-android"
version = "0.1.0"
edition = "2021"
license = "MIT OR Apache-2.0"
description = "Android platform selection gate for Oxide"

[dependencies]
oxide-platform-api = { path = "../platform-api" }

[lib]
path = "src/lib.rs"
