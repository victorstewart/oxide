[package]
name = "oxideui-input"
version = "0.1.0"
edition = "2021"
license = "MIT OR Apache-2.0"
description = "Input mapping and gesture helpers for OxideUI"

[lib]
path = "src/lib.rs"

[dependencies]
oxideui-platform-api = { path = "../platform-api" }
